import React from 'react';

const SectionTitle = ({ children }) => {
  return (
    <h2 className="text-4xl font-bold text-center mb-12 pt-8">
      {children}
    </h2>
  );
};

export default SectionTitle; 