# WorkOS (get from https://dashboard.workos.com)
WORKOS_API_KEY=sk_test_...
WORKOS_CLIENT_ID=client_...
# Optional: custom API host (JWKS URL is https://{host}/sso/jwks/{clientId})
# WORKOS_API_HOSTNAME=api.workos.com
# WORKOS_ISSUER=https://api.workos.com/

# Supabase - use service role key (never expose to frontend)
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Optional: cookie secret for session (if using server-side sessions later)
# COOKIE_PASSWORD=...

# Server port (default 3001)
PORT=3001

# Frontend origin(s) for CORS. One value or comma-separated (e.g. local + Vercel).
# Local only: http://localhost:3000
# Local + production: http://localhost:3000,https://your-app.vercel.app
FRONTEND_ORIGIN=http://localhost:3000
